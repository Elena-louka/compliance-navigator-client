import React from 'react';
import './TopBar.css';

function TopBar() {
    return (
        <div className="top-bar">
            <h1>Compliance Navigator</h1>
            {/* Add more elements to your top bar if needed */}
        </div>
    );
}

export default TopBar;
